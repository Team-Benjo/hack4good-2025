export interface User {
  id: string;
  name: string;
  email: string;
  dob: string;
  image: string;
}

export interface Product {
  name: string;
  price: number;
  quantity: number;
  image: string;
}